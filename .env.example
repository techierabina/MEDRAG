# Copy this file and rename it to .env
# Fill in your actual API key — never share or commit your .env file to GitHub
# Your .env is already in .gitignore so it will never be pushed accidentally

GROQ_API_KEY=your_groq_api_key_here
